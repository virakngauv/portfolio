# Local runtime; frontends remain in their existing repositories.
PORT=8080
GATEWAY_HOST=0.0.0.0
PIC_MATCH_PORT=3200
SECRET_HITMAN_PORT=3225
PIC_MATCH_HOSTS=pic-match.localhost
SECRET_HITMAN_HOSTS=secret-hitman.localhost
PIC_MATCH_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
SECRET_HITMAN_ALLOWED_ORIGINS=http://localhost:3001,http://127.0.0.1:3001
# Enable digitalocean ONLY behind its ingress, never on a public direct server.
CLIENT_IP_MODE=direct
LOG_LEVEL=info
STARTUP_TIMEOUT_MS=30000
SHUTDOWN_TIMEOUT_MS=8000
# Posted by ChatGPT Chat on behalf of @virakngauv.
